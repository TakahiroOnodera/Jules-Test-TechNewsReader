version: '3.8'

services:
  web:
    # Dockerfileがあるカレントディレクトリをビルドコンテキストとして使用
    build: .
    # コンテナのポート5000をホストのポート5000にマッピング
    ports:
      - "5000:5000"
    # ソースコードの変更をコンテナに即時反映させるためのボリュームマウント
    # ホストのカレントディレクトリをコンテナの/appにマウント
    volumes:
      - .:/app
    # 環境変数 (デバッグモードを有効にするなど)
    environment:
      - FLASK_ENV=development
